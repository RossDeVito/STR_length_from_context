# --- Paths ---
data_path: "data/STR_data/HipSTR_labeled_STRs/str_len_4_n_flanking_10000.tsv"
ref_path: "data/STR_data/reference_genome/GRCh38_full_analysis_set_plus_decoy_hla.fa"
experiment_name: "caduceus_v0/str4/str4_f5000_len_v4"

# --- Data ---
batch_size: 16
accumulate_grad_batches: 16
dataloader_num_workers: 4
n_flanking_bp: 5000
n_str_bp: 8

# Single objective (length only). With one target, loss_weighting defaults to "sum".
targets:
  length: mode_copy_number

# --- Model ---
n_prefix_prompt_tokens: 16
n_str_prompt_tokens: 16
n_suffix_prompt_tokens: 16

head_hidden_layers: [256, 128]
head_dropout: 0.2

use_attention_pooling: True
attention_pooling_num_heads: 4

# --- Optimizer (two LRs: backbone low, new heads/tokens higher) ---
optimizer_name: "AdamW"
lr: 1e-4
backbone_lr: 1e-5
weight_decay: 0.05

# --- Scheduler ---
scheduler_name: "ReduceLROnPlateau"
scheduler_patience: 3
scheduler_factor: 0.5
scheduler_cooldown: 2

# --- Trainer ---
max_epochs: 500
patience_early_stopping: 10
gradient_clip_val: 1.0
precision: "bf16-mixed"
